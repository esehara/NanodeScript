h2= title
  　Log一覧
#nav
  a(href="/")戻る
ul
  for date in logs_date
    li
      a(href="/log/" + utils.render_date(date).getFullYear() + (plus_zero(utils.render_date(date).getMonth() + 1)) + (plus_zero(utils.render_date(date).getDate())))
        !{utils.render_date(date).getFullYear()}/!{utils.render_date(date).getMonth() + 1}/!{utils.render_date(date).getDate()}
#nav
  a(href="/")戻る
